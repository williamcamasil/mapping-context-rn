/**
 * Função de exemplo de um "parser".
 *
 * Converte String para Number
 *
 * @param value Texto com valor numérico
 * @returns Valor convertido para Number.
 */
function stringToNumber(value: string) {
  return Number(value);
}

export default stringToNumber;
